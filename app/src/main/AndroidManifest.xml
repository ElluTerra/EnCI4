<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.plataform.enci4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".tenthActivity" />
        <activity android:name=".ninethActivity" />
        <activity android:name=".eighthyActivity" />
        <activity android:name=".sixthyActivity" />
        <activity android:name=".seventhyActivity" />
        <activity android:name=".fiftyActivity" />
        <activity android:name=".FourthyActivity" />
        <activity
            android:name=".Activity_fourthy"
            android:label="@string/title_activity_fourthy"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ThirtyActivity" />
        <activity android:name=".IdentActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>